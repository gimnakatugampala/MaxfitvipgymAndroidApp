<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- Required for foreground service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />



    <uses-feature
        android:name="android.hardware.sensor.stepcounter"
        android:required="false" />






    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaxfitvipgymApp"
        tools:targetApi="31">

        <service
            android:name=".Service.WorkoutForegroundService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <service
            android:name=".Service.HealthTrackerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="health" />

        <!-- ✅ Widget Receiver -->
        <receiver
            android:name=".Widget.WorkoutWidgetProvider"
            android:exported="true"
            android:label="Workout Streak">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>


        <!-- ✅ SPLASH ACTIVITY - NEW LAUNCHER -->
        <activity
            android:name=".Activity.SplashActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".Activity.MainActivity"
            android:exported="false" />

        <activity android:name=".Activity.GetStartedActivity"/>
        <activity android:name=".Activity.PhoneNumberActivity"/>
        <activity android:name=".Activity.VerificationPhoneNumberActivity"/>
        <activity android:name=".Activity.MembershipDetailsActivity"/>
        <activity android:name=".Activity.WorkoutActivity"/>
        <activity android:name=".Activity.TestConnectionActivity"/>
        <activity android:name=".Activity.DebugMemberCreationActivity"/>
        <activity android:name=".Activity.WaitingApprovalActivity"/>
        <activity android:name=".Activity.WorkoutSettingsActivity"/>

    </application>

</manifest>